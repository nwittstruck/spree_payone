<fieldset data-hook="cash_in_advance">
  <legend><%= t(:cash_in_advance) %></legend>

  <table class="index">
    <tr>
      <th><%= t(:details, scope: 'payone') %></th>
    </tr>
  </table>
</fieldset>